# Patch Notes

이 문서는 사용자에게 영향을 주는 기능 추가, 버그 수정, 계산 로직 변경, 배포 내역을 기록합니다.

## 2026-08-14

### 샘플 중심 배틀 조정 워크스페이스 (기능 브랜치)

- 기본 화면에 `샘플 비교` 탭과 모든 탭에서 사용하는 활성 샘플 도구 모음을 추가했습니다.
- 포켓몬, 성격, 6개 Stat Points, 특성 ON/OFF, 지닌 도구, 공격 기술 4개를 `BattleSample`로 저장하도록 구현했습니다.
- 명시적 저장, 다른 이름으로 저장, 새 샘플, 복제, 삭제와 미저장 변경 보호 확인창을 추가했습니다.
- 버전이 포함된 `localStorage` 저장, 손상·구버전 데이터 검증 및 기본 샘플 복구를 추가했습니다.
- 저장 샘플을 벤치마크로 고정해 양방향 전체 기술 데미지, 최종 스피드, 확정 KO·생존·추월 최소 SP를 비교하도록 구현했습니다.
- 현재 배분에서 남은 포인트를 추가하는 최소안과 전체 재분배 최소안을 함께 표시합니다.
- 공격·수비·스피드 계산기가 활성 샘플의 포켓몬, 성격, SP, 특성, 도구를 즉시 공유하도록 변경했습니다.
- 화력·스피드·속임수주사위 도구를 하나의 샘플 도구 목록으로 통합하고 메가폼은 메가스톤 보유 상태로 고정했습니다.
- PokeAPI 기반 특성 한글 이름 200개를 정적 데이터로 추가하고 `천정부지`, `불꽃갈기` 최신 이름을 검증해 보강했습니다.
- 계산 라이브러리에 없는 `천정부지`, `불꽃갈기`, `용화`, `메가솔`의 데미지 관련 효과를 챔피언스 보정 계층에 추가했습니다.
- 모바일에서 비교 카드를 요약과 기술별 상세 펼침 구조로 표시하고 저장 확인창과 도구 모음을 반응형으로 구성했습니다.

### 기능 브랜치 검증

- `npm test`: 11개 파일, 76개 테스트 통과
- `npm run lint`: 통과
- `npm run build`: 통과
- 브랜치: `feature/next-implementation`
- `main` 병합 및 GitHub Pages 배포: 진행하지 않음

### 변경 사항

- 스피드 결과표의 모호한 `원본` 표기를 `기본 실수치`로 변경했습니다.
- 기본 실수치는 레벨 50, 성격, Stat Points를 반영하고 랭크·도구·직접 배율은 적용하기 전의 스피드임을 명확히 했습니다.

### 검증

- `npm test`: 7개 파일, 58개 테스트 통과
- `npm run lint`: 통과
- `npm run build`: 통과

### 배포

- 커밋: `eb5cfa2 Clarify base speed value label`
- GitHub Pages 배포 성공 확인
- 배포 주소: https://jin7369.github.io/pokemon-calculator/

## 2026-07-14

### 변경 사항

- 공격·수비·스피드 계산기의 모든 능력 포인트 초기값을 `0P`로 변경했습니다.
- 페이지 간 공유되는 선택 포켓몬과 스피드 비교 대상도 능력 포인트를 미리 분배하지 않고 시작하도록 변경했습니다.
- Pokemon Champions 전용 종족값 오버라이드 데이터 구조를 추가했습니다.
- 메가 아쿠스타가 `LEGENDS Z-A` 기준 공격 종족값 140과 `천하장사`를 동시에 적용받던 문제를 수정했습니다.
- 메가 아쿠스타의 Champions 종족값을 `60-100-105-130-105-120 (합계 620)`으로 적용했습니다.
- 종족값 오버라이드를 포켓몬 목록, 공격 계산, 수비 계산, 스피드 계산에 공통 적용했습니다.
- 메가 아쿠스타의 종족값과 천하장사 적용 여부를 확인하는 회귀 테스트를 추가했습니다.

### 검증

- `npm test`: 7개 파일, 58개 테스트 통과
- `npm run lint`: 통과
- `npm run build`: 통과

### 배포

- 커밋: `7ff2842 Fix Champions Mega Starmie stats`
- 커밋: `be4c173 Start stat points at zero`
- GitHub Pages 배포 성공 확인
- 배포 주소: https://jin7369.github.io/pokemon-calculator/

## 2026-06-30

### 변경 사항

- Pokemon Champions 능력치 투자 상한을 능력치당 `31P`에서 `32P`로 수정했습니다.
- 선택 포켓몬의 표시 실수치를 Pokemon Champions 전용 공식으로 계산하도록 변경했습니다.
- 데미지 계산 엔진에 전달하는 값은 `0-31P = Stat Point × 8 EV`, `32P = 252 EV`로 환산하도록 조정했습니다.
- 기본 샘플 투자값과 `최대` 표기를 32P 기준으로 갱신했습니다.
- 카디나르마 인게임 스크린샷에서 역산한 실수치 예시를 회귀 테스트에 추가했습니다.

### 검증

- `npm test`: 7개 파일, 56개 테스트 통과
- `npm run lint`: 통과
- `npm run build`: 통과

### 배포

- 커밋: `9606f8f Update Champions stat point calculation`
- GitHub Pages 배포 성공 확인

## 2026-06-29

### 변경 사항

- 공격 계산기의 `공통 방어 조건`에 `피격 대상 도구 보유` 토글을 추가했습니다.
- 수비 계산기의 `방어 설정`에 `피격 포켓몬 도구 보유` 토글을 추가했습니다.
- 대상이 도구를 들고 있는 조건이 켜진 경우, 내부 계산에서 중립 도구를 적용해 도구 보유 판정을 반영하도록 했습니다.
- `폴터가이스트`가 대상 도구 없음 상태에서 0 데미지로 계산되던 원인을 명시적으로 제어할 수 있게 했습니다.
- 같은 도구 보유 조건이 `탁쳐서떨구기`처럼 대상 도구 유무가 중요한 기술에도 반영되도록 했습니다.

### 검증

- `npm test`: 7개 파일, 54개 테스트 통과
- `npm run lint`: 통과
- `npm run build`: 통과

### 배포

- 커밋: `bf7b09f Handle target held item condition`
- GitHub Pages 배포 성공 확인
- 배포 주소: https://jin7369.github.io/pokemon-calculator/
