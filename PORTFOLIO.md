# Portfolio: Pokemon Champions Battle Calculator

## 프로젝트 개요

Pokemon Champions Battle Calculator는 Pokemon Champions 룰 기준으로 포켓몬 한 마리의 공격, 수비, 스피드 보정을 빠르게 실험하기 위한 웹 기반 배틀 스탯 계산기입니다.

- 배포: https://jin7369.github.io/pokemon-calculator/
- 저장소: https://github.com/jin7369/pokemon-calculator
- 형태: React 기반 PWA 정적 웹 앱
- 역할: 기획, 데이터 정리, 계산 로직, UI 구현, 테스트, 배포까지 단독 구현

## 문제 정의

기존 범용 포켓몬 계산기는 개별 데미지 계산에는 강하지만, 특정 룰에서 한 마리 포켓몬의 샘플을 조정할 때 반복 입력이 많습니다.

이 프로젝트는 다음 문제를 해결하는 데 초점을 맞췄습니다.

- Pokemon Champions 출전 가능 포켓몬 기준으로 계산 대상을 제한해야 한다.
- 공격, 수비, 스피드 조정이 서로 분리되면 실제 샘플 조정 흐름과 맞지 않는다.
- 기술 한글명, 포켓몬 한글명, 습득 가능 기술을 룰 기준으로 안정적으로 매칭해야 한다.
- 모바일에서도 검색, 선택, 결과 비교가 빠르게 이뤄져야 한다.

핵심 목표는 단순한 데미지 계산기가 아니라, "한 마리 포켓몬의 실전 샘플을 빠르게 설계하는 도구"를 만드는 것이었습니다.

## 주요 기능

### 공통 포켓몬 빌드

공격, 수비, 스피드 탭은 하나의 선택 포켓몬, 성격, Stat Points를 공유합니다.

- 공격 탭에서 포켓몬을 바꾸면 수비/스피드 탭에도 같은 포켓몬이 반영됩니다.
- 성격 변경은 모든 탭에 공유됩니다.
- Stat Points는 전체 66포인트 제한을 공유합니다.
- 공격 탭은 공격/특공, 수비 탭은 HP/방어/특방, 스피드 탭은 스피드를 조정합니다.

이 구조를 통해 사용자는 탭마다 새로 입력하지 않고 하나의 샘플을 점진적으로 다듬을 수 있습니다.

### 공격 계산기

- 선택 포켓몬이 배울 수 있는 공격 기술만 표시
- 공격 특성 선택 및 ON/OFF 적용
- 화력 아이템, 직접 배율, 최종 배율 표시
- 다단히트 기술의 히트 수, 스킬링크, 속임수주사위 처리
- 전체 출전 가능 포켓몬을 대상으로 확정 생존, 난수, 확정 KO 분류

### 수비 계산기

- 피격 포켓몬의 내구 보정을 기준으로 계산
- 선택한 기술을 배울 수 있는 공격자 목록을 기준으로 데미지 산출
- 공격자 공통 조건으로 성격, 공격 Stat Points, 랭크, 아이템, 직접 배율 지정

### 스피드 계산기

- 기준 포켓몬과 전체 비교 대상의 스피드 계산
- 도구/효과, 랭크, 직접 배율 반영
- 추월, 동속, 추월당함 분류
- 기본 정렬을 "추월 여유 낮은순"으로 두어 실전 조정에 필요한 최소 투자량을 찾기 쉽게 구성

### 모바일 친화 UI

- 포켓몬/기술 선택창을 커스텀 검색 UI로 구현
- 목록은 10개 단위 페이지네이션 적용
- 포켓몬 선택 목록에 타입과 종족값 표시
- 결과 표도 페이지 단위로 탐색 가능

## 기술 스택

- React
- TypeScript
- Vite
- Vitest
- oxlint
- Playwright
- GitHub Actions
- GitHub Pages
- PWA Service Worker
- `@smogon/calc`
- `@pkmn/dex`
- `@pkmn/data`

## 설계와 구현

### 도메인 로직 분리

계산 로직은 UI 컴포넌트에 직접 넣지 않고 `src/domain` 아래로 분리했습니다.

- `damage.ts`: 공격/수비 데미지 계산과 결과 분류
- `speed.ts`: 스피드 계산과 추월 판정
- `statPoints.ts`: Stat Points 제한과 데미지 엔진용 EV 환산
- `multiHit.ts`: 다단히트 기술 처리
- `battleStats.ts`: 선택 포켓몬의 Pokemon Champions 실수치 계산
- `pokemonData.ts`: 포켓몬, 기술, 성격, 이름 매칭 데이터 조합

이 구조는 계산 규칙을 UI 변경과 독립적으로 검증하기 위한 선택입니다.

### Pokemon Champions 룰셋 관리

출전 가능 포켓몬 목록은 `src/data/championsRulesets.ts`에 분리했습니다.

- 공식 페이지 기준 포켓몬 목록
- 별도 리전/폼
- 계산 편의를 위한 전투 폼
- 메가진화 포함 여부

Pokemon Champions 룰이 바뀌어도 한 파일 중심으로 수정할 수 있게 구성했습니다.

### 정적 데이터 생성

런타임에서 외부 API를 호출하지 않도록 포켓몬/기술/특성/습득기 데이터를 정적 파일로 생성합니다.

- PokeAPI 기반 한글 이름 매칭
- 기술명 override
- 포켓몬별 공격 기술 습득 데이터 생성
- Champions 룰 기준 검증 스크립트 제공

이 방식은 배포 후 응답 속도와 오프라인 동작 안정성을 높입니다.

### PWA와 GitHub Pages 배포

앱은 정적 빌드 결과물을 GitHub Pages에 배포합니다.

- `main` push 시 GitHub Actions 자동 배포
- manifest와 service worker 구성
- 모바일 홈 화면 추가 가능
- 오프라인 재방문을 위한 앱 셸 캐시

배포 중 GitHub Pages 설정, private repository 제한, Actions 환경 보호 규칙, Linux optional dependency 문제를 해결했습니다.

## 기술적 의사결정

### 한 마리 포켓몬 빌드를 공유 상태로 관리

초기에는 공격, 수비, 스피드 탭이 각각 포켓몬과 성격, Stat Points를 따로 관리했습니다. 하지만 실제 사용 흐름은 한 마리의 샘플을 여러 관점에서 조정하는 것이므로, 공통 포켓몬 빌드를 상위 상태로 올렸습니다.

그 결과 각 탭은 독립 계산 화면이면서도 같은 샘플을 바라보게 되었고, 샘플 설계 도구로서의 성격이 명확해졌습니다.

### 룰 기반 필터링을 데이터로 분리

Champions 룰 포켓몬 목록과 계산용 파생 폼을 코드 로직에 흩뿌리지 않고 데이터 파일에 모았습니다. 룰 변경 대응 비용을 낮추기 위한 선택입니다.

### 성능 최적화

- 검색 입력 debounce 적용
- 계산 입력 debounce 적용
- 긴 목록과 결과 표에 페이지네이션 적용
- 런타임 API 호출 제거
- 계산 결과 정렬/필터링을 `useMemo`로 제한

### 정확성 우선

계산기류 도구는 UI보다 신뢰성이 먼저입니다. 그래서 기능 추가마다 다음 검증을 반복했습니다.

- 단위 테스트
- lint
- production build
- 브라우저 렌더링 확인
- 모바일 viewport 확인
- GitHub Actions 배포 확인

## 검증

현재 기준 검증 항목:

- `npm test`: 52 tests passed
- `npm run lint`: 통과
- `npm run build`: 통과
- GitHub Pages 배포 성공
- 모바일 390px 및 데스크톱 viewport 렌더링 확인
- 공통 포켓몬 빌드가 공격/수비/스피드 탭 간 공유되는지 Playwright로 확인

## 결과와 가치

이 프로젝트의 가치는 대규모 서비스보다는 좁은 문제를 정확히 푸는 실전형 도구라는 점에 있습니다.

- 특정 룰 기반 계산
- 데이터 정합성 관리
- 도메인 로직과 UI 분리
- 모바일 사용성
- PWA 배포
- 테스트와 라이선스 고지

단순 CRUD가 아니라 실제 사용자의 반복 작업을 줄이는 계산/데이터 중심 앱이라는 점에서 포트폴리오 프로젝트로 설명력이 있습니다.

## 향후 개선 계획

- 샘플 저장 기능
- 공유 링크 기능
- 날씨, 필드, 벽, 크리티컬 등 전투 조건 확장
- 방어 측 특성 적용
- 룰 업데이트 자동 검증 강화
- 포켓몬/기술 데이터 변경 이력 표시
- 결과를 바탕으로 추천 Stat Points 역산 기능
- E2E 테스트 확대

## 배운 점

이 프로젝트에서 가장 중요한 설계 포인트는 "계산 기능을 많이 넣는 것"보다 "사용자가 실제로 조정하는 사고 흐름에 맞게 상태를 설계하는 것"이었습니다.

공격, 수비, 스피드는 기능적으로는 다른 계산이지만, 사용자는 결국 한 마리 포켓몬의 샘플을 만들고 있습니다. 이 관점을 반영해 공통 포켓몬 빌드 상태를 도입하면서 도구의 방향성이 더 선명해졌습니다.
